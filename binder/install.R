# Pacotes instalados no momento em que o Binder constrói o ambiente.
# (O kernel de R para o Jupyter — IRkernel — é instalado automaticamente pelo Binder.)
install.packages("bibliometrix")
install.packages("bibliometrixData")
